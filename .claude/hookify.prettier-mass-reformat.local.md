---
name: warn-prettier-mass-reformat
enabled: true
event: bash
pattern: prettier[^\n]*--write
---

🎨 **Масове форматування ламає 19 тестових сюїт. Це вимірювано, не гіпотеза.**

Тут **69 тестових файлів читають вихідний текст модулів** (`readFileSync` +
`toContain`) і звіряють точні рядки — з відступами й переносами. Prettier
переносить рядки, і звірка не сходиться.

Заміряно на цьому репозиторії (prettier 3, конфіг, який відтоді видалено):

| | Сюїт червоних | Тестів червоних |
|---|---|---|
| чистий код | 7 (відомі, з `CLAUDE.md`) | 18 |
| після `--write` по `src` | 26 | 43 |

345 із 398 файлів не відповідали тому конфігу; диф — близько 46 000 рядків.
`.prettierrc.json` **видалено навмисно** — не повертайте його: редактор із
format-on-save читає такий файл і переформатовує збережене мовчки.

**Що робити натомість:** форматувати вручну той шматок, який пишете, у стилі
сусіднього коду. Якщо форматування таки потрібне — спершу полагодьте тести, що
читають джерело, і лише потім чіпайте `src`.

/**
 * Типографіка нотаток — одна на приватну і на публічну.
 *
 * Це два записи про одну людину, зроблені в одному місці й прочитані одним
 * поглядом; різнити їх розміром шрифту нема за чим. А різнились вони саме ним:
 * приватна нотатка була `textarea`, що успадковувала розмір тіла картки, а
 * публічна жила на власних 12.3px — і плейсхолдери двох сусідніх полів
 * виглядали як з різних застосунків.
 *
 * Числа лежать окремим модулем, бо стилі цих двох блоків живуть у різних
 * файлах (`Matching.styled.jsx` і `MatchingHiddenList.styled.jsx`). Спільний
 * імпорт — єдиний спосіб, у який вони не розійдуться знову.
 */

/** Текст нотатки й підказка в порожньому полі. */
export const NOTE_TEXT_SIZE = '13.5px';
export const NOTE_TEXT_LINE_HEIGHT = 1.55;

/** Рядок підпису над нотаткою: хто її бачить, чий це запис, коли зроблений. */
export const NOTE_META_SIZE = '11.5px';
export const NOTE_META_LINE_HEIGHT = 1.4;

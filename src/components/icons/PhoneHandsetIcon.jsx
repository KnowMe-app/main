import React from 'react';

/**
 * Телефонна трубка.
 *
 * Своя, а не `FaPhoneVolume`: у тієї з боків стоять звукові хвилі, тож у ряду
 * з рештою значків контактів вона єдина «шуміла» — важила більше за сусідів і
 * читалась як інша сім'я знаків. Тут лишилась сама трубка.
 *
 * Пропси й поведінка ті самі, що в `react-icons`: розмір за замовчуванням
 * `1em` (тобто його задає шрифт або CSS навколо), колір — `currentColor`.
 * Завдяки цьому компонент підставляється в наявні місця без жодних правок
 * розмітки й підхоплює `svg { width: 17px }` зі стилів картки.
 */
export const PhoneHandsetIcon = ({ size = '1em', title, ...rest }) => (
  <svg
    viewBox="0 0 24 24"
    width={size}
    height={size}
    fill="currentColor"
    role={title ? 'img' : 'presentation'}
    aria-hidden={title ? undefined : 'true'}
    focusable="false"
    {...rest}
  >
    {title && <title>{title}</title>}
    {/* Трубка йде по діагоналі: слухавка внизу ліворуч, мікрофон угорі
        праворуч. Кути заокруглені тим самим радіусом, що й у решти значків
        набору, щоб у ряду вони читались як одна сім'я. */}
    <path d="M7.6 3H4.2C3.5 3 3 3.5 3 4.2 3 13.5 10.5 21 19.8 21c.7 0 1.2-.5 1.2-1.2v-3.4c0-.7-.5-1.2-1.2-1.2-1.2 0-2.4-.2-3.5-.6-.4-.1-.9 0-1.2.3l-1.9 1.9a15.6 15.6 0 0 1-6.5-6.5l1.9-1.9c.3-.3.4-.8.3-1.2-.4-1.1-.6-2.3-.6-3.5C8.8 3.5 8.3 3 7.6 3z" />
  </svg>
);

export default PhoneHandsetIcon;
